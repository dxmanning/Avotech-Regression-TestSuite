# CSI (RCSI) — copy to .env and set password locally (do not commit .env)

# Optional; if unset, defaults in config/csi.ts apply
CSI_BASE_URL=""
CSI_TEST_EMAIL=""
CSI_TEST_PASSWORD=""

# Org admin (Account Management — user list / add user, AB-019)
CSI_ORG_TEST_EMAIL=""
CSI_ORG_TEST_PASSWORD=""
